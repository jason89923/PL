<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <title>OurC</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            text-align: center; /* 置中頁面內容 */
            font-family: Arial, sans-serif; /* 字體樣式 */
        }
        h1 {
            margin-top: 50px; /* 標題上方間距 */
        }
        .buttons {
            margin-top: 20px; /* 按鈕上方間距 */
        }
        button {
            margin: 10px; /* 按鈕間距 */
            padding: 10px 20px; /* 按鈕內邊距 */
            font-size: 16px; /* 文字大小 */
            cursor: pointer; /* 鼠標樣式 */
            display: inline-block; /* 確保按鈕能夠正確排列 */
            width: auto; /* 自動寬度 */
        }
        /* 媒體查詢：當螢幕尺寸小於或等於600px時 */
        @media (max-width: 600px) {
            h1 {
                margin-top: 20px; /* 減少標題上方間距 */
            }
            .buttons {
                margin-top: 10px; /* 減少按鈕上方間距 */
            }
            button {
                margin: 5px; /* 減少按鈕間距 */
                padding: 5px 10px; /* 減少按鈕內邊距 */
                font-size: 14px; /* 減小文字大小 */
            }
        }
    </style>
</head>
<body>
    <h1>OurC</h1>
    <div class="buttons">
        <button onclick="location.href='/OurC_program/1'">project1</button>
        <button onclick="location.href='/OurC_program/2'">project2</button>
        <button onclick="location.href='/OurC_program/3'">project3</button>
        <button onclick="location.href='/OurC_program/4'">project4</button>
    </div>
</body>
</html>
